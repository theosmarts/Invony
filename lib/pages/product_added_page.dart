//Packages

import 'package:flutter/material.dart';

class ProductAddedPage extends StatelessWidget {
  const ProductAddedPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
